export class BlogUpdaterWrapper {
	static publishImage(imageName: string): boolean {
		return false;
	}

	static updateImageRelease(imageName: string, release: boolean): boolean {
		return false;
	}

	static checkIfImageExists(imageName: string): boolean {
		return false;
	}
}
